<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">

	<!-- 회원가입 -->
	<insert id="insert" parameterType="uservo">
	<![CDATA[
			insert into member 
			values(null, #{id}, #{email}, #{pw}, #{name}, #{nickname}, #{addr} ,#{tell_ph}, #{cell_ph}, now(), 100, 0, #{birthday}, #{account_number}, 0, 1, #{email_recv}, #{sms_recv}, 0, false)
		]]>
		<!-- 넘어오는 uservo에 no를 셋팅해준다. -->
		<selectKey keyProperty="no" resultType="long" order="AFTER">
		<![CDATA[
			select last_insert_id()		
		]]>
		</selectKey>
	</insert>

	<select id="loginUser" parameterType="uservo" resultType="uservo">
	<![CDATA[
		select no from member where id=#{id} and pw=#{pw}
	]]>
	</select>

	<!-- ID 유무 검사 -->
	<select id="userIdCheck" parameterType="uservo"
		resultType="uservo">	
		<![CDATA[
			select id from member where id = #{id}		
		]]>
	</select>


</mapper>